[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "mcp-yandex-dostavka"
version = "0.0.1"
description = "MCP-сервер для Яндекс Доставки (dostavka.yandex.ru) — расчёт стоимости, заявки и трекинг отправлений. Для Claude, Cursor, Codex и других AI-клиентов."
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.10"
authors = [{ name = "gistrec", email = "gistrec@gmail.com" }]
keywords = ["mcp", "yandex", "dostavka", "delivery"]
classifiers = [
    "Development Status :: 1 - Planning",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
]

[project.urls]
Homepage = "https://github.com/A1-x-Tech/mcp-yandex-dostavka"
Repository = "https://github.com/A1-x-Tech/mcp-yandex-dostavka"

[tool.hatch.build.targets.wheel]
packages = ["mcp_yandex_dostavka"]
